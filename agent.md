# Project Agent Instructions

## Project

Drone management frontend client. Uses Orval-generated React Query hooks from backend OpenAPI spec, with Better Auth cookie-based sessions.

## Stack

- Bun 1.3 (package manager + TS runner)
- Vite 8 + React 19
- TypeScript 5.9
- TanStack Router 1.95 (file-based, auto-code-splitting)
- TanStack Query 5 (server state, cache, mutations)
- Orval (generated React Query hooks from OpenAPI)
- `better-auth/react` (auth client state)
- Tailwind CSS v4 + Radix UI primitives (shadcn-style)
- Tauri 2 (desktop + Android, notifications)
- Vitest + Testing Library (tests)
- oxlint (linter) + Prettier (formatter)

## Source layout

```
src/
  app/              Router setup + type registration
  components/ui/    Reusable UI primitives
  features/auth/    Better Auth client + AuthForm component
  features/drones/  Drone API hooks, mutations, CRUD components
  features/notifications/  Tauri notification helpers
  lib/api/          Orval-generated API, mutator, models
  lib/env.ts        Single env source
  routes/           Thin route files (TanStack Router file-based)
```

## Hard rules

- **Use Bun** for all scripts (`bun run dev`, `bun run build`, `bun run test`, etc.).
- Never edit `src/routeTree.gen.ts` — it is auto-generated by TanStack Router plugin.
- Never hand-edit `src/lib/api/generated/**` or `.orval-spec.json` — regenerate via `bun run gen:types`.
- Keep route files (`src/routes/`) thin; import features from `src/features/*`.
- Do not refactor beyond what the task requires. Do not add new dependencies unless asked.
- Do not commit or push unless explicitly requested.

## API/Auth rules

- `VITE_API_URL` must be **origin only** (e.g. `http://localhost:3050`). Never append `/api`.
- Empty `VITE_API_URL` = use same-origin Vite/nginx proxy (default).
- Vite dev proxy: `/api` → `http://localhost:3050`, `/auth` → `http://localhost:3050/api/auth`.
- All API calls use `credentials: 'include'` (handled by `customInstance` mutator in `src/lib/api/mutator.ts`).
- Auth client is `better-auth/react` via `src/features/auth/auth-client.ts`.
- After drone mutations, invalidate `dronesKeys.all` to refetch.
- `bun run gen:types` fetches OpenAPI from `http://localhost:9009/api-json`, strips `^/api/auth/{path}$` via `jq`, runs Orval. Requires `jq`.

## Routing and data rules

- Register router type in `src/app/router.ts` so `Link`/`useNavigate`/`useParams` are type-safe.
- Query keys live in feature `api/keys.ts` (e.g. `dronesKeys`). Detail key uses `uuid: string`.
- Use generated hooks from `src/lib/api/generated/**` (e.g. `useGetDrones`, `useCreateDrone`).
- Route loaders can use `context.queryClient.ensureQueryData` with `getDrones` for prefetching.
- Feature hooks in `src/features/drones/hooks/` wrap generated hooks with invalidation logic.

## Verification

- Run `bun run lint` before finishing.
- Run `bun run typecheck` after any TypeScript changes.
- Run `bun run build` for a full production build check.
- Run `bun run test` if touching feature code.
- Run `bun run format:check` for formatting.

## What not to do

- Do not edit generated files (`routeTree.gen.ts`, `src/lib/api/generated/**`, `.orval-spec.json`).
- Do not add npm/yarn/pnpm lockfiles.
- Do not refactor components/ui/ unless specifically asked.
- Do not inline VITE_API_URL with `/api`.
- Do not skip running `bun run gen:types` when the backend API contract changes.
